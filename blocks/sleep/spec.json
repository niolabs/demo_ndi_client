{
  "nio/Sleep": {
    "version": "0.1.0",
    "description": "Sleep for a given interval before passing signals",
    "categories": [
      "Signal Flow"
    ],
    "properties": {
      "backup_interval": {
        "title": "Backup Interval",
        "type": "TimeDeltaType",
        "description": "Interval to back up signals to persistence",
        "default": {
          "seconds": 3600
        }
      },
      "interval": {
        "title": "Interval",
        "type": "TimeDeltaType",
        "description": "How long to wait before notifying signals",
        "default": {
          "seconds": 1
        }
      },
      "load_from_persistence": {
        "title": "Load from Persistence?",
        "type": "BoolType",
        "description": "Whether to load persisted signals from persistence",
        "default": true
      }
    },
    "inputs": {
      "default": {
        "description": "Any list of signals"
      }
    },
    "outputs": {
      "default": {
        "description": "Same list of signals after specified interval"
      }
    },
    "commands": {}
  }
}