{
  "nio/AttributeSelector": {
    "version": "1.0.0",
    "description": "This block is used to whitelist or blacklist incoming signal attributes and notify the resulting modified signals.",
    "categories": [
      "Signal Modifier"
    ],
    "properties": {
      "attributes": {
        "title": "Incoming signal attributes",
        "type": "ListType",
        "description": "Specify any incoming signal attributes to ignore or allow depending on specified behavior.",
        "default": []
      },
      "mode": {
        "title": "Selector Mode",
        "type": "SelectType",
        "description": "Specify whitelist or blacklist behavior.",
        "default": false
      }
    },
    "inputs": {
      "default": {
        "description": "Any list of signals"
      }
    },
    "outputs": {
      "default": {
        "description": "The input list of signals but with modified attributes depending on whitelist/blacklist selections."
      }
    },
    "commands": {}
  }
}