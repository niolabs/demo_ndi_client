{
    "exclude": false,
    "fields": [
        {
            "formula": "{{ [n for n in range(3, $max, 2) if all(n%i!=0 for i in range(2, int(math.sqrt(n))+1))] }}",
            "title": "primes"
        }
    ],
    "log_level": "NOTSET",
    "name": "CalculatePrimeNumbers",
    "type": "Modifier",
    "version": "1.0.0"
}