{
    "exclude": true,
    "fields": [
        {
            "formula": "{{ int($length) }}",
            "title": "length"
        },
        {
            "formula": "{{ int($length*__import__('math').log($length)+$length*__import__('math').log(__import__('math').log($length))) }}",
            "title": "max"
        },
        {
            "formula": "{{ $time }}",
            "title": "time"
        }
    ],
    "log_level": "NOTSET",
    "name": "ConvertValues",
    "type": "Modifier",
    "version": "1.0.0"
}