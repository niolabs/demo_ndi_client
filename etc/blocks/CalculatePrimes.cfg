{
    "exclude": true,
    "fields": [
        {
            "formula": "{{ [x for x in range(1, math.floor($primes*math.log($primes) + ($primes-1)*math.log(math.log($primes))) ) if x > 1 and all(x%i for i in __import__('itertools').islice(__import__('itertools').count(2), int(__import__('math').sqrt(x)-1)))] }}",
            "title": "all_primes"
        },
        {
            "formula": "done",
            "title": "message"
        }
    ],
    "log_level": "NOTSET",
    "name": "CalculatePrimes",
    "type": "Modifier",
    "version": "1.0.0"
}