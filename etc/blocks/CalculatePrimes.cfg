{
    "exclude": true,
    "fields": [
        {
            "formula": "{{ False if len([n for n in range(2, int(math.sqrt(int($number)))+1) if int($number)%n == 0]) > 0 else True }}",
            "title": "is_prime"
        },
        {
            "formula": "{{ $length }}",
            "title": "length"
        },
        {
            "formula": "{{ int($number) }}",
            "title": "number"
        }
    ],
    "log_level": "NOTSET",
    "name": "CalculatePrimes",
    "type": "Modifier",
    "version": "1.0.0"
}